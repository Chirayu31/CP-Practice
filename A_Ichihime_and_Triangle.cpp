#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int arr[4];
        cin >> arr[0] >> arr[1] >> arr[2] >> arr[3];
        cout << arr[1] << " " << arr[2] << " " << arr[2] << endl;
    }
}