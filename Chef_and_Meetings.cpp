#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string p;
        getline(cin, p);
        int n;
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            string lr;
            getline(cin, lr);
        }
    }
}