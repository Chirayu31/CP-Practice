#include <bits/stdc++.h>
using namespace std;

int solve()
{
    int n, m;
    cin >> n >> m;

    vector<int> a(n), b(m);

    for (int i = 0; i < n; i++)
        cin >> a[i];

    for (int i = 0; i < m; i++)
        cin >> b[i];

    int D = 0;
    vector<int> c(n);

    for (int i = 0; i < n; i++)
    {
    }
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        cout << solve() << "\n";
    }
}